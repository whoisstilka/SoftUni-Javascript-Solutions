function concataneNames(firstName, secondName, delimiter) {
    console.log(`${firstName}${delimiter}${secondName}`);
}

concataneNames('John', 'Smith', '->');
concataneNames('Jan', 'White', '<->');
concataneNames('Linda', 'Terry', '=>');