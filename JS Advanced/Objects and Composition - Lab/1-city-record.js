function city(name, population, treasury) {
    const result = {
        name: name, // name
        population: population, // population
        treasury: treasury // treasury
    };

    return result;
}

console.log(city('Tortuga', 7000, 15000));
console.log(city('Santa Domingo', 12000, 23500));